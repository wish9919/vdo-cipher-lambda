service: vdo-cipher-demo
frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs14.x
  stage: dev
  region: us-east-1
  environment:
    VDO_CIPHER_SECRET: ${file(./config.${opt:stage, 'dev'}.json):VDO_CIPHER_SECRET}

plugins:
  - "serverless-offline"

functions:
  api:
    handler: handler.api
    events:
      - http:
          path: /api/videos/{id}/otp
          method: post
          cors: true
      - http:
          path: /api/videos
          method: get
          cors: true
      - http:
          path: /api/videos/{id}
          method: get
          cors: true
      - http:
          path: /api/videos
          method: put
          cors: true
      - http:
          path: /api/videos/{id}
          method: put
          cors: true
  #    Define function environment variables here
# resources:
#   Resources:
#     NewResource:
#       Type: AWS::S3::Bucket
#       Properties:
#         BucketName: vdo-cipher-demo-bucket
